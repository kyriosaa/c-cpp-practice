#include <stdio.h>

int main() {
    /*

     + (addition)
     - (subtraction)
     * (multiplication)
     / (division)
     % (modulus)
     ++ (increment by 1)
     -- (decrement by 1)

    */

   int x = 8;
   int y = 5;

   float z = x / (float) y; // because division is weird

   printf("%f", z);

   return 0;
}